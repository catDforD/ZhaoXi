# 任务完成检查清单

## 代码提交前

### 代码质量
- [ ] 代码遵循项目类型定义（`src/types/index.ts`）
- [ ] 使用正确的命名规范（PascalCase/camelCase）
- [ ] TypeScript 类型正确，无 `any` 滥用
- [ ] 使用 `@/` 路径别名而非相对路径

### 代码检查
```bash
cd app
npm run lint
```
确保没有 ESLint 错误。

### 功能验证
- [ ] 功能在开发模式下正常工作 (`npm run tauri:dev`)
- [ ] 相关页面可以正确渲染
- [ ] 状态更新正确
- [ ] 与 Rust 后端的通信正常（如适用）

## 不需要做的事情
- 不需要手动修改 shadcn/ui 组件（位于 `src/components/ui/`）
- 不需要添加额外的错误处理，除非这是系统边界（用户输入、外部 API）
- 不需要为一次性操作创建辅助函数或抽象

## 提交规范
- 使用中文或英文提交信息
- 遵循项目的提交信息风格
